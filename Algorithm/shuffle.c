#include<stdio.h>
#include<stdlib.h>
#include<time.h>

main()
{
	int i = 0, t, w;
	int data[20];
	srand(time(0);
	rand();
	for (i = 0; i < 20; i++) {
		printf("data[i]",i + 1);
	}
	i=0
		while (i < 20)
		{

			data[i]
		}
}